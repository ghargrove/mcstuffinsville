{
  "name": "mcstuffinsville",
  "version": "1.0.0",
  "description": "Patient finder for olive",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "tslint -c tslint.json 'src/**/*.ts'",
    "start:server": "ts-node --project src/server/tsconfig.json src/server/index.ts"
  },
  "author": "Graham Hargrove",
  "license": "ISC",
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "*.{ts,tsx}": [
      "prettier --write",
      "npm run lint"
    ]
  },
  "devDependencies": {
    "@types/express": "^4.17.3",
    "husky": "^4.2.3",
    "lint-staged": "^10.0.8",
    "prettier": "^1.19.1",
    "ts-node": "^8.6.2",
    "tslint": "^6.1.0",
    "typescript": "^3.8.3"
  },
  "dependencies": {
    "apollo-server-express": "^2.11.0",
    "express": "^4.17.1"
  }
}
